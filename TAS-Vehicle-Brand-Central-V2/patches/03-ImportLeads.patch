diff --git a/client/src/pages/ImportLeads.tsx b/client/src/pages/ImportLeads.tsx
index 3e075e6..ed23b70 100644
--- a/client/src/pages/ImportLeads.tsx
+++ b/client/src/pages/ImportLeads.tsx
@@ -205,7 +205,9 @@ export default function ImportLeads() {
 
   const { data: campaigns } = trpc.campaigns.list.useQuery();
   const { data: agents } = trpc.tas.sales.listAssignableSalesAgents.useQuery(undefined);
+  const { data: vehicleBrands } = trpc.tas.vehicleBrands.list.useQuery({ activeOnly: true });
   const [selectedCampaign, setSelectedCampaign] = useState<string>("");
+  const [selectedBrandId, setSelectedBrandId] = useState<string>("");
   const [selectedAgentIds, setSelectedAgentIds] = useState<number[]>([]);
 
   const queueProgressQuery = trpc.tas.sales.excelQueueProgress.useQuery(
@@ -262,6 +264,10 @@ export default function ImportLeads() {
   }, [isRTL]);
 
   const handleImport = () => {
+    if (!selectedBrandId) {
+      toast.error(isRTL ? "اختر ماركة السيارة قبل بدء الاستيراد" : "Select a vehicle brand before starting the import");
+      return;
+    }
     const leadsData = rows.map((row) => {
       const lead: Record<string, any> = {};
       for (const [header, field] of Object.entries(mapping)) {
@@ -290,6 +296,7 @@ export default function ImportLeads() {
       assignmentMode,
       selectedAgentIds,
       sourceFileName: fileName,
+      brandId: Number(selectedBrandId),
     });
   };
 
@@ -477,7 +484,7 @@ export default function ImportLeads() {
                 </h2>
               </div>
               <div className="p-6">
-                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
+                <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                   <div>
                     <label className="text-xs font-semibold text-slate-600 block mb-1.5">
                       {isRTL ? "الحملة الإعلانية" : "Campaign"}
@@ -494,6 +501,28 @@ export default function ImportLeads() {
                       </SelectContent>
                     </Select>
                   </div>
+                  <div>
+                    <label className="text-xs font-semibold text-slate-600 block mb-1.5">
+                      {isRTL ? "ماركة السيارة *" : "Vehicle Brand *"}
+                    </label>
+                    <Select value={selectedBrandId} onValueChange={setSelectedBrandId}>
+                      <SelectTrigger className="h-9 text-sm border-slate-200">
+                        <SelectValue placeholder={isRTL ? "اختر الماركة..." : "Select brand..."} />
+                      </SelectTrigger>
+                      <SelectContent>
+                        {(vehicleBrands ?? []).map((brand: any) => (
+                          <SelectItem key={brand.id} value={String(brand.id)} className="text-sm">
+                            {isRTL ? (brand.nameAr || brand.nameEn) : brand.nameEn}
+                          </SelectItem>
+                        ))}
+                      </SelectContent>
+                    </Select>
+                    {(vehicleBrands ?? []).length === 0 && (
+                      <p className="text-[11px] text-amber-600 mt-1">
+                        {isRTL ? "أضف ماركة نشطة من الإعدادات أولاً" : "Add an active brand from Settings first"}
+                      </p>
+                    )}
+                  </div>
                   <div>
                     <div className="flex items-center justify-between mb-1.5">
                       <label className="text-xs font-semibold text-slate-600">
